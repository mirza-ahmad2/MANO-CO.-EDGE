# Agent notes

Keep commits linear and avoid rewriting published history (force push, rebase, amend, or squash of already-pushed commits) so remote project history stays intact.

Keep the connected branch in a working, buildable state when pushing.
